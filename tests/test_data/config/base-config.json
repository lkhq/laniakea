{
    "ProjectName": "Test",
    "Archive": {
        "path": "???",
        "develSuite": "dasyatis",
        "incomingSuite": "staging",
        "distroTag": "test"
    },

    "Database": {
        "db": "laniakea_test",
        "user": "lkdbuser_test",
        "password": "notReallySecret"
    },

    "Synchrotron": {
      "SourceKeyringDir": "/etc/apt/trusted.gpg.d/"
    },

    "Workspace": "/tmp/test-lkws/",

    "Lighthouse": {
      "endpoints": {
        "jobs": ["tcp://*:5570"],
        "events": ["tcp://*:5571"],
        "publish": ["tcp://*:5572"]
      },

      "servers": {
        "jobs": ["tcp://localhost:5570"],
        "events": ["tcp://localhost:5571"],
        "publish": ["tcp://localhost:5572"]
      }
    }
}
